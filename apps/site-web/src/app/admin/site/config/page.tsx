import { SiteConfigAdminPage } from "@/components/admin/site-config-admin-page"

export const metadata = {
  title: "Site Config",
}

export default function SiteConfigRoutePage() {
  return <SiteConfigAdminPage />
}
